import type { CapacitorConfig } from '@capacitor/cli'; const config: CapacitorConfig = { appId: 'com.attendance.salarytracker', appName: 'متتبع الحضور والراتب', webDir: 'dist', server: { url: 'https://attendance-pro-app.netlify.app', cleartext: false, }, android: { allowMixedContent: false, backgroundColor: '#0f172a', }, plugins: { SplashScreen: { launchShowDuration: 2000, backgroundColor: '#0f172a', showSpinner: false, }, }, }; export default config;
